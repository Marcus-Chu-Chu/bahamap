# Copy this file to .env (same folder) and paste your real key from
# https://console.anthropic.com -> API Keys. .env is gitignored; never commit a key.
ANTHROPIC_API_KEY=sk-ant-your-key-here
# Only needed for identity-linked keys (Console Settings -> Workspaces -> copy ID):
ANTHROPIC_WORKSPACE_ID=
