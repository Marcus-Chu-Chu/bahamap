# Tool configuration only — the project installs via requirements.txt.

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "BLE"]
# Long lines are almost all prompt/template strings; wrapping them would hurt readability.
ignore = ["E501"]
